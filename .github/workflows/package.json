{
  "name": "euaqui",
  "version": "1.0.0",
  "description": "EuAqui — Sua rede, seu mundo",
  "type": "module",
  "scripts": {
    "build": "tsc && node build.mjs",
    "dev": "npx http-server dist -p 3000 -o",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
